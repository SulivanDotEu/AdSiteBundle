{% extends  'WalvaCrudAdminBundle:Crud:index.html.twig' %}

{% block crud_add_button %}
    <div class="pull-right">
        <a href="{{ path(routeAdd) }}" class="btn btn-default">
            <span class="glyphicon glyphicon-plus"></span> Créer une campagne
        </a>
    </div>
{% endblock %}

 {% block crud_table_header %}
     <th>#</th>
     <th>Nom</th>
     <th>Objectif</th>
     <th>Date</th>
     <th>Action</th>
 {% endblock %}

{% block crud_table_row %}
    <tr>
        <td><a href="{{ path(routeShow, { 'id': entity.id }) }}">{{ entity.id }}</a></td>
        <td>{{ 'Nom' }}</td>
        <td>{{ 'Objectif' }}</td>
        <td>{{ 'Date' }}</td>
        <td>
            <div class="btn-group">
                <a href="{{ path(routeShow, { 'id': entity.id }) }}" class="btn btn-default btn-xs"><span
                            class="glyphicon glyphicon-new-window"></span> Show</a>
                <a href="{{ path(routeEdit, { 'id': entity.id }) }}" class="btn btn-default btn-xs"><span
                            class="glyphicon glyphicon-pencil"></span> Edit</a>
            </div>
        </td>
    </tr>
{% endblock %}


{% block actions %}
    <div class="btn-group">
        <a href="{{ path(routeShow, { 'id': entity.id }) }}" class="btn btn-default btn-xs"><span
                    class="glyphicon glyphicon-new-window"></span> Show</a>
        <a href="{{ path(routeEdit, { 'id': entity.id }) }}" class="btn btn-default btn-xs"><span
                    class="glyphicon glyphicon-pencil"></span> Edit</a>
    </div>
{% endblock %}

